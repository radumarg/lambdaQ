The Language LambdaQ
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of LambdaQ==

===Literals===
Double-precision float literals //Double// have the structure
indicated by the regular expression ``digit+ '.' digit+ ('e' ('-')? digit+)?`` i.e.\
two sequences of digits separated by a decimal point, optionally
followed by an unsigned or negative exponent.


Integer literals //Integer// are nonempty sequences of digits.






GateVar literals are recognized by the regular expression
`````upper ('_' | digit | upper)*`````

Var literals are recognized by the regular expression
`````('_' | lower) (["'_"] | digit | letter)*`````

Lambda literals are recognized by the regular expression
`````'\'`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in LambdaQ are the following:
  | ``Bit`` | ``Bool`` | ``FSWAP`` | ``False``
  | ``H`` | ``ID`` | ``ISWAP`` | ``Int``
  | ``QFT`` | ``QFT_DAG`` | ``Qbit`` | ``ROOT_SWAP``
  | ``ROOT_SWAP_DAG`` | ``ROOT_X`` | ``ROOT_X_DAG`` | ``ROOT_Y``
  | ``ROOT_Y_DAG`` | ``ROOT_Z`` | ``ROOT_Z_DAG`` | ``RX``
  | ``RY`` | ``RZ`` | ``S`` | ``SQRT_SWAP``
  | ``SQRT_SWAP_DAG`` | ``SQRT_X`` | ``SQRT_X_DAG`` | ``SQRT_Y``
  | ``SQRT_Y_DAG`` | ``SWAP`` | ``SWAP_THETA`` | ``S_DAG``
  | ``T`` | ``T_DAG`` | ``True`` | ``U1``
  | ``U2`` | ``U3`` | ``X`` | ``Y``
  | ``Z`` | ``case`` | ``ctrl`` | ``else``
  | ``gate`` | ``if`` | ``in`` | ``let``
  | ``not`` | ``of`` | ``then`` | ``with``

The symbols used in LambdaQ are the following:
  | - | + | * | /
  | ( | ) | && | ||
  | == | /= | > | >=
  | < | <= | -> | **
  | ! | () | [ | ]
  | @0 | @1 | @+ | @-
  | @+i | @-i | , | !!
  | . | { | = | }
  | <- | ; | $ | []
  | ++ | : | :: |

===Comments===
Single-line comments begin with --.Multiple-line comments are  enclosed with {- and -}.

==The syntactic structure of LambdaQ==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //[FunctionDeclaration]//
  | //IntegerExpression1// | -> | ``-`` //IntegerExpression//
  |  |  **|**  | //IntegerExpression1// ``+`` //IntegerExpression2//
  |  |  **|**  | //IntegerExpression1// ``-`` //IntegerExpression2//
  |  |  **|**  | //IntegerExpression2//
  | //IntegerExpression2// | -> | //IntegerExpression2// ``*`` //IntegerExpression3//
  |  |  **|**  | //IntegerExpression2// ``/`` //IntegerExpression3//
  |  |  **|**  | //IntegerExpression3//
  | //IntegerExpression3// | -> | //Integer//
  |  |  **|**  | ``(`` //IntegerExpression// ``)``
  | //IntegerExpression// | -> | //IntegerExpression1//
  | //BoolValue// | -> | ``True``
  |  |  **|**  | ``False``
  | //BoolExpression// | -> | //BoolExpression// ``&&`` //BoolExpression1//
  |  |  **|**  | //BoolExpression// ``||`` //BoolExpression1//
  |  |  **|**  | ``not`` //BoolExpression1//
  |  |  **|**  | //BoolExpression1//
  | //BoolExpression1// | -> | //BoolExpression1// ``==`` //BoolExpression2//
  |  |  **|**  | //BoolExpression1// ``/=`` //BoolExpression2//
  |  |  **|**  | //BoolExpression2//
  | //BoolExpression2// | -> | //IntegerExpression2// ``==`` //IntegerExpression2//
  |  |  **|**  | //IntegerExpression2// ``/=`` //IntegerExpression2//
  |  |  **|**  | //IntegerExpression2// ``>`` //IntegerExpression2//
  |  |  **|**  | //IntegerExpression2// ``>=`` //IntegerExpression2//
  |  |  **|**  | //IntegerExpression2// ``<`` //IntegerExpression2//
  |  |  **|**  | //IntegerExpression2// ``<=`` //IntegerExpression2//
  |  |  **|**  | //BoolValue//
  |  |  **|**  | ``(`` //BoolExpression// ``)``
  | //Type// | -> | //Type// ``->`` //Type1//
  |  |  **|**  | //Type1//
  | //Type1// | -> | //Type1// ``+`` //Type2//
  |  |  **|**  | //Type2//
  | //Type2// | -> | //Type2// ``*`` //Type3//
  |  |  **|**  | //Type3//
  | //Type3// | -> | //Type4// ``**`` //Integer//
  |  |  **|**  | ``!`` //Type4//
  |  |  **|**  | //Type4//
  | //Type5// | -> | ``Bool``
  |  |  **|**  | ``Bit``
  |  |  **|**  | ``Int``
  |  |  **|**  | ``Qbit``
  |  |  **|**  | ``()``
  |  |  **|**  | ``[`` //Type// ``]``
  |  |  **|**  | ``(`` //Type// ``)``
  | //Type4// | -> | //Type5//
  | //Angle// | -> | //Double//
  | //BasisState// | -> | ``@0``
  |  |  **|**  | ``@1``
  |  |  **|**  | ``@+``
  |  |  **|**  | ``@-``
  |  |  **|**  | ``@+i``
  |  |  **|**  | ``@-i``
  | //Gate// | -> | ``H``
  |  |  **|**  | ``X``
  |  |  **|**  | ``Y``
  |  |  **|**  | ``Z``
  |  |  **|**  | ``ID``
  |  |  **|**  | ``ROOT_X`` //Integer//
  |  |  **|**  | ``ROOT_X_DAG`` //Integer//
  |  |  **|**  | ``ROOT_Y`` //Integer//
  |  |  **|**  | ``ROOT_Y_DAG`` //Integer//
  |  |  **|**  | ``ROOT_Z`` //Integer//
  |  |  **|**  | ``ROOT_Z_DAG`` //Integer//
  |  |  **|**  | ``S``
  |  |  **|**  | ``S_DAG``
  |  |  **|**  | ``T``
  |  |  **|**  | ``T_DAG``
  |  |  **|**  | ``SQRT_X``
  |  |  **|**  | ``SQRT_X_DAG``
  |  |  **|**  | ``SQRT_Y``
  |  |  **|**  | ``SQRT_Y_DAG``
  |  |  **|**  | ``RX`` //Angle//
  |  |  **|**  | ``RY`` //Angle//
  |  |  **|**  | ``RZ`` //Angle//
  |  |  **|**  | ``U1`` //Angle//
  |  |  **|**  | ``U2`` ``(`` //Angle// ``,`` //Angle// ``)``
  |  |  **|**  | ``U3`` ``(`` //Angle// ``,`` //Angle// ``,`` //Angle// ``)``
  |  |  **|**  | ``SWAP``
  |  |  **|**  | ``SQRT_SWAP``
  |  |  **|**  | ``SQRT_SWAP_DAG``
  |  |  **|**  | ``ISWAP``
  |  |  **|**  | ``FSWAP``
  |  |  **|**  | ``SWAP_THETA`` //Angle//
  |  |  **|**  | ``ROOT_SWAP`` //Integer//
  |  |  **|**  | ``ROOT_SWAP_DAG`` //Integer//
  |  |  **|**  | ``QFT`` //Integer//
  |  |  **|**  | ``QFT_DAG`` //Integer//
  |  |  **|**  | //GateVar// ``(`` //Angle// ``,`` //Angle// ``,`` //Angle// ``)``
  |  |  **|**  | //GateVar// ``(`` //Angle// ``,`` //Angle// ``)``
  |  |  **|**  | //GateVar// //Angle//
  |  |  **|**  | //GateVar// //Integer//
  |  |  **|**  | //GateVar//
  | //[Var]// | -> | //Var//
  |  |  **|**  | //Var// ``,`` //[Var]//
  | //ControlBasisState// | -> | ``[`` //BasisState// ``]``
  | //ControlBasisStates// | -> | ``[`` //BasisState// ``,`` //[BasisState]// ``]``
  | //[BasisState]// | -> | //BasisState//
  |  |  **|**  | //BasisState// ``,`` //[BasisState]//
  | //ControlBit// | -> | ``[`` //Integer// ``]``
  | //ControlBits// | -> | ``[`` //Integer// ``,`` //[Integer]// ``]``
  | //[Integer]// | -> | //Integer//
  |  |  **|**  | //Integer// ``,`` //[Integer]//
  | //ControlTerm// | -> | ``[`` //Term// ``]``
  | //ControlTerms// | -> | ``[`` //Term// ``,`` //[Term]// ``]``
  | //ControlVar// | -> | ``[`` //Var// ``]``
  | //ControlVars// | -> | ``[`` //Var// ``,`` //[Var]// ``]``
  | //[Term]// | -> | //Term//
  |  |  **|**  | //Term// ``,`` //[Term]//
  | //Term4// | -> | //List// ``!!`` //Integer//
  |  |  **|**  | //List//
  |  |  **|**  | ``(`` //Term// ``)``
  | //Term3// | -> | ``()``
  |  |  **|**  | //BasisState//
  |  |  **|**  | //BoolExpression//
  |  |  **|**  | //IntegerExpression//
  |  |  **|**  | ``gate`` //Gate//
  |  |  **|**  | //Var//
  |  |  **|**  | ``(`` //Term// ``,`` //[Term]// ``)``
  |  |  **|**  | ``(`` //Var// ``,`` //[Var]// ``)``
  |  |  **|**  | //Term4//
  | //Term2// | -> | ``with`` //ControlTerm// ``ctrl`` //ControlBasisState//
  |  |  **|**  | ``with`` //ControlTerms// ``ctrl`` //ControlBasisStates//
  |  |  **|**  | ``with`` //ControlVars// ``ctrl`` //ControlBasisStates//
  |  |  **|**  | ``with`` //ControlTerm// ``ctrl`` //ControlBit//
  |  |  **|**  | ``with`` //ControlTerms// ``ctrl`` //ControlBits//
  |  |  **|**  | ``with`` //ControlVars// ``ctrl`` //ControlBits//
  |  |  **|**  | //Var// ``,`` //[Var]//
  |  |  **|**  | //Term2// //Term3//
  |  |  **|**  | //Term2// ``.`` //Term3//
  |  |  **|**  | //Term3//
  | //Term1// | -> | ``if`` //Term// ``then`` //Term// ``else`` //Term//
  |  |  **|**  | ``let`` ``{`` //Var// ``=`` //Term// ``}`` ``in`` //Term//
  |  |  **|**  | ``let`` ``{`` ``(`` //Var// ``,`` //[Var]// ``)`` ``=`` //Term// ``}`` ``in`` //Term//
  |  |  **|**  | //Var// ``<-`` //Term// ``;`` //Term//
  |  |  **|**  | //Var// ``,`` //[Var]// ``<-`` //Term// ``;`` //Term//
  |  |  **|**  | ``case`` //Term// ``of`` ``{`` //[CaseExpression]// ``}``
  |  |  **|**  | //Lambda// //Var// //Type// ``.`` //Term//
  |  |  **|**  | //Term2//
  | //Term// | -> | //Term1// ``$`` //Term//
  |  |  **|**  | //Term1//
  | //List1// | -> | ``[]``
  |  |  **|**  | ``[`` //Term// ``]``
  |  |  **|**  | ``[`` //Term// ``,`` //[Term]// ``]``
  |  |  **|**  | ``(`` //List// ``)``
  | //List// | -> | //List// ``++`` //List1//
  |  |  **|**  | //Term3// ``:`` //List1//
  |  |  **|**  | //List1//
  | //CaseExpression// | -> | //Term// ``->`` //Term//
  |  |  **|**  | //CaseExpression// ``;``
  | //[CaseExpression]// | -> | //CaseExpression//
  |  |  **|**  | //CaseExpression// //[CaseExpression]//
  | //Arg// | -> | //Var//
  | //[Arg]// | -> | **eps**
  |  |  **|**  | //Arg// //[Arg]//
  | //FunctionDefinition// | -> | //Var// //[Arg]// ``=`` //Term//
  |  |  **|**  | //FunctionDefinition// ``;``
  | //FunctionType// | -> | //Var// ``::`` //Type//
  |  |  **|**  | //FunctionType// ``;``
  | //FunctionDeclaration// | -> | //FunctionType// ``;`` //FunctionDefinition// ``;``
  | //[FunctionDeclaration]// | -> | **eps**
  |  |  **|**  | //FunctionDeclaration// //[FunctionDeclaration]//



%% File generated by the BNF Converter (bnfc 2.9.5).

The Language LambdaQ
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of LambdaQ==

===Literals===
Double-precision float literals //Double// have the structure
indicated by the regular expression ``digit+ '.' digit+ ('e' ('-')? digit+)?`` i.e.\
two sequences of digits separated by a decimal point, optionally
followed by an unsigned or negative exponent.


Integer literals //Integer// are nonempty sequences of digits.







GateIdent literals are recognized by the regular expression
`````upper ('_' | digit | letter)*`````

Var literals are recognized by the regular expression
`````('_' | lower) (["'_"] | digit | letter)*`````

Lambda literals are recognized by the regular expression
`````'\'`````

Bit literals are recognized by the regular expression
`````["01"]`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in LambdaQ are the following:
  | ``Bit`` | ``FSWAP`` | ``H`` | ``ID``
  | ``ISWAP`` | ``Qbit`` | ``ROOT_SWAP`` | ``ROOT_SWAP_DAG``
  | ``ROOT_X`` | ``ROOT_X_DAG`` | ``ROOT_Y`` | ``ROOT_Y_DAG``
  | ``ROOT_Z`` | ``ROOT_Z_DAG`` | ``RX`` | ``RY``
  | ``RZ`` | ``S`` | ``SQRT_SWAP`` | ``SQRT_SWAP_DAG``
  | ``SQRT_X`` | ``SQRT_X_DAG`` | ``SQRT_Y`` | ``SQRT_Y_DAG``
  | ``SWAP`` | ``SWAP_THETA`` | ``S_DAG`` | ``T``
  | ``T_DAG`` | ``U1`` | ``U2`` | ``U3``
  | ``X`` | ``Y`` | ``Z`` | ``case``
  | ``else`` | ``gate`` | ``if`` | ``in``
  | ``let`` | ``of`` | ``then`` | ``with``

The symbols used in LambdaQ are the following:
  | () | ! | ** | *
  | -> | ( | ) | @0
  | @1 | @+ | @- | @+i
  | @-i | , | ctrl-gate | [
  | ] | { | = | }
  | $ | ; | :: |

===Comments===
Single-line comments begin with --.Multiple-line comments are  enclosed with {- and -}.

==The syntactic structure of LambdaQ==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Program// | -> | //[FunctionDeclaration]//
  | //Type3// | -> | ``Bit``
  |  |  **|**  | ``Qbit``
  |  |  **|**  | ``()``
  |  |  **|**  | ``(`` //Type// ``)``
  | //Type2// | -> | ``!`` //Type3//
  |  |  **|**  | //Type3//
  | //Type1// | -> | //Type2// ``**`` //Integer//
  |  |  **|**  | //Type2// ``*`` //Type1//
  |  |  **|**  | //Type2//
  | //Type// | -> | //Type1// ``->`` //Type//
  |  |  **|**  | //Type1//
  | //Angle// | -> | //Double//
  | //ControlState// | -> | ``@0``
  |  |  **|**  | ``@1``
  |  |  **|**  | ``@+``
  |  |  **|**  | ``@-``
  |  |  **|**  | ``@+i``
  |  |  **|**  | ``@-i``
  | //Control// | -> | //Term// //ControlState//
  | //[Control]// | -> | //Control//
  |  |  **|**  | //Control// ``,`` //[Control]//
  | //Gate// | -> | ``gate`` ``H`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``H`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``X`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``X`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``Y`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``Y`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``Z`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``Z`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``ID`` //Term//
  |  |  **|**  | ``gate`` ``ROOT_X`` //Integer// //Term//
  |  |  **|**  | ``ctrl-gate`` ``ROOT_X`` //Integer// //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``ROOT_X_DAG`` //Integer// //Term//
  |  |  **|**  | ``ctrl-gate`` ``ROOT_X_DAG`` //Integer// //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``ROOT_Y`` //Integer// //Term//
  |  |  **|**  | ``ctrl-gate`` ``ROOT_Y`` //Integer// //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``ROOT_Y_DAG`` //Integer// //Term//
  |  |  **|**  | ``ctrl-gate`` ``ROOT_Y_DAG`` //Integer// //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``ROOT_Z`` //Integer// //Term//
  |  |  **|**  | ``ctrl-gate`` ``ROOT_Z`` //Integer// //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``ROOT_Z_DAG`` //Integer// //Term//
  |  |  **|**  | ``ctrl-gate`` ``ROOT_Z_DAG`` //Integer// //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``S`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``S`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``S_DAG`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``S_DAG`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``T`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``T`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``T_DAG`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``T_DAG`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``SQRT_X`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``SQRT_X`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``SQRT_X_DAG`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``SQRT_X_DAG`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``SQRT_Y`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``SQRT_Y`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``SQRT_Y_DAG`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``SQRT_Y_DAG`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``RX`` //Angle// //Term//
  |  |  **|**  | ``ctrl-gate`` ``RX`` //Angle// //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``RY`` //Angle// //Term//
  |  |  **|**  | ``ctrl-gate`` ``RY`` //Angle// //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``RZ`` //Angle// //Term//
  |  |  **|**  | ``ctrl-gate`` ``RZ`` //Angle// //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``U1`` //Angle// //Term//
  |  |  **|**  | ``ctrl-gate`` ``U1`` //Angle// //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``U2`` ``(`` //Angle// ``,`` //Angle// ``)`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``U2`` ``(`` //Angle// ``,`` //Angle// ``)`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``U3`` ``(`` //Angle// ``,`` //Angle// ``,`` //Angle// ``)`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``U3`` ``(`` //Angle// ``,`` //Angle// ``,`` //Angle// ``)`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``SWAP`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``SWAP`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``SQRT_SWAP`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``SQRT_SWAP`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``SQRT_SWAP_DAG`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``SQRT_SWAP_DAG`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``ISWAP`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``ISWAP`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``FSWAP`` //Term//
  |  |  **|**  | ``ctrl-gate`` ``FSWAP`` //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``SWAP_THETA`` //Term// //Angle//
  |  |  **|**  | ``ctrl-gate`` ``SWAP_THETA`` //Angle// //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``ROOT_SWAP`` //Term// //Integer//
  |  |  **|**  | ``ctrl-gate`` ``ROOT_SWAP`` //Integer// //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` ``ROOT_SWAP_DAG`` //Integer// //Term//
  |  |  **|**  | ``ctrl-gate`` ``ROOT_SWAP_DAG`` //Integer// //Term// ``with`` ``[`` //[Control]// ``]``
  |  |  **|**  | ``gate`` //GateIdent// //Term//
  |  |  **|**  | ``ctrl-gate`` //GateIdent// //Term// ``with`` ``[`` //[Control]// ``]``
  | //LetVariable// | -> | //Var//
  | //[LetVariable]// | -> | //LetVariable//
  |  |  **|**  | //LetVariable// ``,`` //[LetVariable]//
  | //LambdaVariable// | -> | //Var//
  | //[LambdaVariable]// | -> | **eps**
  |  |  **|**  | //LambdaVariable// //[LambdaVariable]//
  | //Tuple// | -> | ``(`` //Term// ``,`` //[Term]// ``)``
  | //[Term]// | -> | //Term//
  |  |  **|**  | //Term// ``,`` //[Term]//
  | //Term3// | -> | //Var//
  |  |  **|**  | //Bit//
  |  |  **|**  | //Gate//
  |  |  **|**  | //Tuple//
  |  |  **|**  | ``()``
  |  |  **|**  | ``(`` //Term// ``)``
  | //Term1// | -> | ``if`` //Term// ``then`` //Term// ``else`` //Term//
  |  |  **|**  | ``let`` ``{`` //LetVariable// ``=`` //Term// ``}`` ``in`` //Term//
  |  |  **|**  | ``let`` ``{`` ``(`` //LetVariable// ``,`` //[LetVariable]// ``)`` ``=`` //Term// ``}`` ``in`` //Term//
  |  |  **|**  | ``case`` //Term// ``of`` //CaseExpression// //[CaseExpression]//
  |  |  **|**  | //Lambda// //LambdaVariable// //[LambdaVariable]// ``->`` //Term//
  |  |  **|**  | //Term2// ``$`` //Term1//
  |  |  **|**  | //Term2//
  | //Term2// | -> | //Term2// //Term3//
  |  |  **|**  | //Term3//
  | //Term// | -> | //Term1//
  | //CaseExpression// | -> | //Term// ``->`` //Var//
  | //[CaseExpression]// | -> | //CaseExpression//
  |  |  **|**  | //CaseExpression// //[CaseExpression]//
  | //Arg// | -> | //Var//
  | //[Arg]// | -> | **eps**
  |  |  **|**  | //Arg// //[Arg]//
  | //FunctionDefinition// | -> | //Var// //[Arg]// ``=`` //Term//
  |  |  **|**  | //FunctionDefinition// ``;``
  | //FunctionType// | -> | //Var// ``::`` //Type//
  |  |  **|**  | //FunctionType// ``;``
  | //FunctionDeclaration// | -> | //FunctionType// ``;`` //FunctionDefinition// ``;``
  | //[FunctionDeclaration]// | -> | **eps**
  |  |  **|**  | //FunctionDeclaration// //[FunctionDeclaration]//



%% File generated by the BNF Converter (bnfc 2.9.4.1).

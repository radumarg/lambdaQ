  -- TODO:
  -- function names are unique
  -- no duplicate function declaration exists
  -- function signatures and definitions have matching names
  -- the number of function arguments match the arguments in the signature
  -- control qubits for controlled gates are different from target qubit

module Backend.SemanticAnalyer where

